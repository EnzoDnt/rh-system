export const BRAND_NAME = (import.meta.env.VITE_BRAND_NAME?.trim() || "Recrutement");
export const BRAND_PRIMARY_COLOR = import.meta.env.VITE_BRAND_PRIMARY_COLOR?.trim() || null;
export const BRAND_PRIMARY_COLOR_HOVER = import.meta.env.VITE_BRAND_PRIMARY_COLOR_HOVER?.trim() || null;
export const BRAND_LOGO_URL = import.meta.env.VITE_BRAND_LOGO_URL?.trim() || null;
